doctype html
html
  head
    meta(name="viewport", content="width=device-width, initial-scale=1")
    title map
    link(rel="stylesheet", href="/css/style.css")
  body
    .controls 
      form#controls 
        label.category-name Category_Name
        input(type="checkbox", name="some_category", value="Active Life", checked=(true === false ? "checked" : undefined)). 
        br
        label.category-name Category_Name
        input(type="checkbox", name="some_category", value="Arts & Entertainment", checked=(true === false ? "checked" : undefined)). 
        br
        label.category-name Category_Name
        input(type="checkbox", name="some_category", value="Automotive", checked=(true === false ? "checked" : undefined)). 
        br
        label.category-name Category_Name
        input(type="checkbox", name="some_category", value="Beauty & Spas", checked=(true === false ? "checked" : undefined)). 
        br
        label.category-name Category_Name
        input(type="checkbox", name="some_category", value="Education", checked=(true === false ? "checked" : undefined)). 
        br
        input#submit(type="submit", value="Submit").
        br
        button#active-life Active Life
    #map
    script(src="https://ajax.googleapis.com/ajax/libs/jquery/2.2.0/jquery.min.js")
    script(type="text/javascript", src="http://maps.google.com/maps/api/js?libraries=visualization")
    script(src="/js/mapStyles.js")
    script(src="/js/script.js")
