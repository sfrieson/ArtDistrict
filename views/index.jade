doctype html
html
  head
    meta(name="viewport", content="width=device-width, initial-scale=1")
    title map
    link(rel="stylesheet", href="/css/style.css")
  body
    .controls 
      form#controls 
        label.category-name Category_Name
        input(type="checkbox", name="some_category", value="category_value", checked=(true === false ? "checked" : undefined)). 
        br
        label.category-name Category_Name
        input(type="checkbox", name="some_category", value="category_value", checked=(true === false ? "checked" : undefined)). 
        br
        label.category-name Category_Name
        input(type="checkbox", name="some_category", value="category_value", checked=(true === false ? "checked" : undefined)). 
        br
        label.category-name Category_Name
        input(type="checkbox", name="some_category", value="category_value", checked=(true === false ? "checked" : undefined)). 
        br
        label.category-name Category_Name
        input(type="checkbox", name="some_category", value="category_value", checked=(true === false ? "checked" : undefined)). 
        br
        input#submit(type="submit", value="Submit").
    #map
    script(src="https://ajax.googleapis.com/ajax/libs/jquery/2.2.0/jquery.min.js")
    script(type="text/javascript", src="http://maps.google.com/maps/api/js?libraries=visualization")
    script(src="/js/mapStyles.js")
    script(src="/js/script.js")
